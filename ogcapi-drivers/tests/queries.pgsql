-- select * from meta.jobs;

delete from meta.jobs;

-- SELECT row_to_json ("status_info!") as "status_info!"
-- FROM (
--     SELECT
--         process_id,
--         job_id,
--         status,
--         message,
--         created,
--         finished,
--         updated,
--         progress,
--         links
--     from meta.jobs
-- ) as "status_info!";
